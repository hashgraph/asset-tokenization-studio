---
"@hashgraph/asset-tokenization-contracts": minor
---

Add `initializeBatchBurn` function to `BatchBurn` facet with centralised registration
via `InitializerStorageWrapper.setFacetToReady`. Emits `BatchBurnInitialized` event.
