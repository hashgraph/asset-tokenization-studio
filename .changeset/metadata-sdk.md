---
"@hashgraph/asset-tokenization-sdk": minor
---

Added `setMetadata` command and `getMetadata` query to the SDK, enabling callers to write and read arbitrary key/value metadata on a security token.
