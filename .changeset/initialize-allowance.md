---
"@hashgraph/asset-tokenization-contracts": minor
---

Add `initializeAllowance` function to `Allowance` facet with centralised registration
via `InitializerStorageWrapper.setFacetToReady`. Emits `AllowanceInitialized` event.
