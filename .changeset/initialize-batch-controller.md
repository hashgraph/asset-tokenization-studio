---
"@hashgraph/asset-tokenization-contracts": minor
---

Add `initializeBatchController` function to `BatchController` facet with centralised registration
via `InitializerStorageWrapper.setFacetToReady`. Emits `BatchControllerInitialized` event.
