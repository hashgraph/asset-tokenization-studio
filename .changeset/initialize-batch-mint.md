---
"@hashgraph/asset-tokenization-contracts": minor
---

Add `initializeBatchMint` function to `BatchMint` facet with centralised registration
via `InitializerStorageWrapper.setFacetToReady`. Emits `BatchMintInitialized` event.
