---
"@hashgraph/asset-tokenization-contracts": minor
---

Add `initializeBatchFreeze` function to `BatchFreeze` facet with centralised registration
via `InitializerStorageWrapper.setFacetToReady`. Emits `BatchFreezeInitialized` event.
