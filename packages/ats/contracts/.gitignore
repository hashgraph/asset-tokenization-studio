# Transpiled files
build

# VS Code
.vscode
!.vscode/tasks.js

# JetBrains IDEs
.idea/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Misc
.DS_Store

# Old files
.old

# history
.history

node_modules
.env

# Docs
docs/
coverage
coverage.json
gas-report.txt
extracted-methods.txt

#Hardhat files
/cache
artifacts

.deps/
./docs/*
./cache/*
./typechain-types/*
typechain
typechain-types

# Auto-generated contract registry data (BBND-1766).
# The heavy registry file is regenerated on every `prepare` / `hardhat compile`.
# The sibling `atsRoles.generated.ts` stays in git (small, audit-relevant).
scripts/domain/atsRegistry.generated.ts

# Legacy auto-generated registry-data mirror. Superseded by atsRegistry.generated.ts;
# kept gitignored so a stale local copy never re-enters version control.
scripts/domain/atsRegistry.data.ts

#Slither
Slither/*
slither.db.json

# Deployment output files - ignore local/test networks
deployments/*hardhat*.json
deployments/*localhost*.json
deployments/*local*.json
deployments/test-*.json

# Local deployment directories (entire directories for local testing)
deployments/local/
deployments/hedera-local/
deployments/test/

# Checkpoint files (deployment state tracking) - never commit checkpoints
deployments/**/.checkpoints/
*.checkpoint.json

# Registry generator cache
.registry-cache.json

# Test artifacts - all test-generated files go here
.temp/

# Engram AI agent memory
.engram/

# Auto-generated MCP server context (added automatically)
.mcp-server-context.md
