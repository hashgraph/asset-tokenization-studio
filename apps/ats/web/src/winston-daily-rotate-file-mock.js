// Winston Daily Rotate File mock for browser compatibility
export default class DailyRotateFile {
    constructor(options = {}) {
        this.options = options;
    }
}

export { DailyRotateFile };
